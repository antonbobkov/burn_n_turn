---
description: When modifying source code or CMake build files, run the project build to verify correctness.
alwaysApply: false
---
After modifying code, build the project to verify correctness. After modifying CMake files, run cmake and build. After modifying tests, run tests to make sure they pass.
